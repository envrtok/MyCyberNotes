- 🎯 **Purpose:** Query DNS servers for information about hostnames, IPs, and records.
- 🛠️ **Use case:** Troubleshooting DNS, checking propagation, inspecting record types.

---

## 🔑 Basic Usage

```bash
dig <hostname>
```

👉 Queries the default DNS server for `A` record of the hostname.

---

## 📋 Common Options

|Option|Meaning|Example|
|---|---|---|
|`@server`|Specify DNS server|`dig @8.8.8.8 example.com`|
|`+short`|Simplified output|`dig +short example.com`|
|`+noall +answer`|Show only answer section|`dig example.com +noall +answer`|
|`+trace`|Trace DNS delegation path|`dig example.com +trace`|
|`+tcp`|Force TCP instead of UDP|`dig example.com +tcp`|
|`+time=<sec>`|Set query timeout|`dig example.com +time=5`|
|`+retry=<n>`|Retry count|`dig example.com +retry=2`|

---

## 📂 Record Types

```bash
dig <hostname> <record_type>
```

- `A` → IPv4 address
- `AAAA` → IPv6 address
- `MX` → Mail exchange servers
- `NS` → Name servers
- `CNAME` → Canonical name (alias)
- `TXT` → Text records (SPF, DKIM, etc.)
- `SOA` → Start of Authority

👉 Example:

```bash
dig example.com MX
```

---

## 🧪 Practical Examples

- Check IP quickly:

```bash
dig +short example.com
```

- Query Google DNS for NS records:

```bash
dig @8.8.8.8 example.com NS
```

- Trace full DNS resolution path:

```bash
dig example.com +trace
```

---

## ⚡ Pro Tips

- Combine with `grep` for filtering:

```bash
dig example.com MX +short | grep mail
```

- Use `+nocmd` to suppress command header.
- Great for scripting DNS checks in automation.