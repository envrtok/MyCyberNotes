### **The `grep` Command** 🔍

The `grep` command finds searching values from files.

- `strings` command returns the string ones from the document. it may be helpful

---

### **Usage** ⚙️

**Command:** `grep <value> <path>`

**Examples:**
- `grep "hello" text.txt`
    - Finds lines from text.txt which includes hello word ✨
- `strings text.txt | grep "\=\=="`
    - Finds lines includes === which strings from text 🔤