## `sort` Command 📈

### Basic Usage
```bash
# Sort a file alphabetically (default) 🔤
sort filename.txt

# Sort and display output 👀
sort file.txt

# Sort in reverse order 🔄
sort -r file.txt

# Sort numerically 🔢
sort -n file.txt

# Sort by specific field (space/tab separated) 🎯
sort -k 2 file.txt        # Sort by 2nd column
sort -k 3,3 file.txt      # Sort by 3rd column only
```

### Common Options ⚙️
```bash
# Remove duplicates while sorting 🗑️
sort -u file.txt

# Case-insensitive sort 🔠
sort -f file.txt

# Sort by numeric value of specific field 🔢🎯
sort -k 2n file.txt       # Sort 2nd column numerically

# Multiple field sorting 🎯🎯
sort -k 1,1 -k 2n file.txt  # Sort by 1st field, then 2nd numerically

# Check if already sorted ✅
sort -c file.txt          # Exit code 0 if sorted, 1 if not

# Human-readable numbers (K, M, G suffixes) 👨‍💼
sort -h file.txt

# Month name sorting 📅
sort -M file.txt
```

### Practical Examples 🛠️
```bash
# Sort a CSV by 3rd column numerically 📊
sort -t, -k3n data.csv

# Sort by multiple columns: name then age 👤🔢
sort -k1,1 -k2,2n people.txt

# Sort IP addresses 🌐
sort -t. -k1,1n -k2,2n -k3,3n -k4,4n ip_list.txt

# Sort with custom delimiter 🔧
sort -t':' -k3n /etc/passwd
```

---

## `uniq` Command 🦄

### Basic Usage
```bash
# Remove consecutive duplicate lines 🗑️
uniq file.txt

# Show only unique lines (appear exactly once) 💎
uniq -u file.txt

# Show only duplicate lines 🔄
uniq -d file.txt

# Count occurrences of each line 📊
uniq -c file.txt
```

### Common Options ⚙️
```bash
# Case-insensitive comparison 🔠
uniq -i file.txt

# Skip first N fields (space/tab separated) ⏭️
uniq -f 2 file.txt       # Skip first 2 fields

# Skip first N characters ⏭️
uniq -s 5 file.txt       # Skip first 5 characters

# Compare only first N characters ✂️
uniq -w 10 file.txt      # Compare only first 10 chars
```

### Important Note ⚠️
`uniq` only removes **consecutive** duplicates. Use with `sort` for complete deduplication:
```bash
sort file.txt | uniq
```

---

## Powerful Combinations 💪🔗

### Most Common Patterns
```bash
# Get unique sorted list 💎📈
sort file.txt | uniq

# Count unique occurrences (sorted by count) 📊🥇
sort file.txt | uniq -c | sort -nr

# Find duplicate lines in a file 🔄🔍
sort file.txt | uniq -d

# Find unique lines only 💎✨
sort file.txt | uniq -u
```

### Advanced Examples 🚀
```bash
# Top 10 most frequent lines 🏆🔟
sort file.txt | uniq -c | sort -nr | head -10

# Remove duplicates from unsorted data 🗑️✨
sort file.txt | uniq > deduplicated.txt

# Case-insensitive unique count 🔠📊
sort -f file.txt | uniq -ic

# Find duplicate IPs in log file 🌐🔍
cut -d' ' -f1 access.log | sort | uniq -d

# Count unique values in column 2 of CSV 📊🎯
cut -d, -f2 data.csv | sort | uniq -c
```

### Real-world Use Cases 🌍
```bash
# Analyze web server logs 🌐📊
cut -d' ' -f1 access.log | sort | uniq -c | sort -nr

# Find duplicate files by checksum 📁🔍
find . -type f -exec md5sum {} \; | sort | uniq -d -w32

# Process CSV data 📊🔧
cut -d, -f3 employees.csv | sort | uniq -c

# Clean word list 📝✨
sort words.txt | uniq > clean_words.txt

# Find most common error messages 🚨📊
grep "ERROR" app.log | sort | uniq -c | sort -nr | head -5
```

## Quick Reference Table 📋

| Command                       | Purpose                       | Common Use             |
| ----------------------------- | ----------------------------- | ---------------------- |
| `sort file`                   | Sort alphabetically           | Basic sorting          |
| `sort -n file`                | Sort numerically              | Number lists           |
| `sort -r file`                | Reverse sort                  | Z-A or high-low        |
| `sort -u file`                | Sort and remove duplicates    | Clean data             |
| `uniq file`                   | Remove consecutive duplicates | Process sorted data    |
| `uniq -c file`                | Count occurrences             | Frequency analysis     |
| `sort \| uniq`                | Remove all duplicates         | Complete deduplication |
| `sort \| uniq -c \| sort -nr` | Show frequency ranking        | Top N analysis         |

## Pro Tips 💡

1. **Always remember**: `uniq` needs sorted input! 📈 → 🦄
2. **Pipe magic**: Combine with `head`, `tail`, `grep` for powerful data analysis 🔗✨
3. **Memory efficient**: Great for large files that can't fit in memory 💾🚀
4. **Debug**: Use `sort -c` to check if your data is already sorted 🐛✅

## Memory Aid 🧠
> "**Sort** first, then **uniq** - that's the workflow trick!" 🎵
> 
> "**Consecutive** duplicates only - that's uniq's story!" 📖

Remember: **Always sort before uniq** unless you specifically want only consecutive duplicates removed! ⚠️✨

**Happy data processing!** 🎉🐧