### 📂 Path & Navigation

- `cd` → gives current path
- `cd <target directory>` → changes current path
- `cd ..` → changes current path to upper path

---

### 📑 Listing Files

- `dir` → shows files in the current path
    - `dir /a` → shows **all** files in the current path
    - `dir /s` → shows files and subfiles in the current path
- `tree` → shows files in the current path, like a tree

---

### 📁 Folder Operations

- `mkdir <name>` → creates folder
- `rmdir <name>` → deletes folder

---

### 📄 File Operations

- `more <file>` → reads file
- `copy <file> <copy-one>` → copies file
- `move <file> <new path>` → moves file
- `del <file>` → deletes file
- `erase <file>` → deletes file

---

### ✨ Wildcards

- `*` → means all files in the current directory
