### 📋 Listing Tasks

- `tasklist` → lists all tasks

```cmd
C:\>tasklist
```

---

### 🔎 Filtering Tasks

- `tasklist F1 "imagename eq <service name>"` → finds tasks related to the service

```cmd
C:\>tasklist F1 "imagename eq <service name>"
```

---

### ❌ Killing Tasks

- `taskkill /PID <pid>` → kills task

```cmd
C:\>taskkill /PID <pid>
```
