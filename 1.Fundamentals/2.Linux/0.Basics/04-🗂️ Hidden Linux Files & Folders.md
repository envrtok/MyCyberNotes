## 🐚 Shell & Terminal

- **`.bashrc`** → Shell settings, aliases, PATH, prompt customization
- **`.bash_profile` / `.profile`** → Environment variables loaded at login
- **`.bash_history`** → Command history (`history` command shows it)

## 🔐 Security & Keys

- **`.ssh/`** → SSH keys and connection configs
- **`.gnupg/`** → GPG keys and encryption settings

## 🛠️ Application Configs

- **`.gitconfig`** → Git user info and aliases
- **`.vimrc`** → Vim editor configuration
- **`.config/`** → Modern app configs (e.g. `nvim`, `htop`)
- **`.mozilla/`** → Firefox profiles, extensions, settings

## 📦 User-Specific Data

- **`.local/`**
    - `bin/` → User-installed programs
    - `lib/` → Libraries
    - `share/` → Shared data
- **`.cache/`** → Temporary data (browser cache, thumbnails)

---

## 📊 Quick Table

|Category|File/Folder|Purpose|
|---|---|---|
|Shell|`.bashrc`|Aliases, PATH, prompt|
|Shell|`.bash_profile`|Environment variables|
|Shell|`.bash_history`|Command history|
|Security|`.ssh/`|SSH keys|
|Security|`.gnupg/`|GPG keys|
|Application|`.gitconfig`|Git settings|
|Application|`.vimrc`|Vim settings|
|Application|`.config/`|App configs|
|Application|`.mozilla/`|Firefox profiles|
|User Data|`.local/`|Programs, libs, shared data|
|User Data|`.cache/`|Temporary data|
